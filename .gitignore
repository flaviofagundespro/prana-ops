# Dependencies
node_modules/

# Build output
dist/
build/

# Local data — the SQLite database holds SSH profile paths; never commit it
server/data/*.db
server/data/*.db-journal
server/data/*.db-wal

# Environment & secrets
.env
.env.local
.env.*.local
*.key
*.pem

# Logs
*.log
logs/

# Editor / OS
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp

# AI agent tooling — local development configuration, not project content
.claude/
.codex/
.cursor/
.agents/
CLAUDE.md
AGENTS.md
